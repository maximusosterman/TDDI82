#include "counted_ptr.h"
#include "counted_ptr.tcc" // If definitions are in .tcc

#include <iostream>

int main() {

    // CountedPtr<int> p {new int{42}};
    // std::cout << "*p = " << *p << "\n";


    return 0;
}
